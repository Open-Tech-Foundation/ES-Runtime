# {{name}}

React on [ES Runtime](https://esrun.opentechf.org), rendered by a server of its own.

```sh
npm install
npm run dev     # http://localhost:8080
npm test
npm run build   # dist/server.js and the page it serves
npm start       # runs the build with only the permissions it needs
```
