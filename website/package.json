{
  "name": "website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "otfw dev",
    "build": "otfw build --ssg",
    "preview": "otfw build --ssg && wrangler dev",
    "deploy": "otfw build --ssg && wrangler deploy"
  },
  "dependencies": {
    "@opentf/web": "^0.26.0",
    "@opentf/web-docs": "^0.23.0"
  },
  "devDependencies": {
    "@opentf/web-cli": "^1.24.0",
    "wrangler": "^4.118.0"
  },
  "trustedDependencies": [
    "@opentf/web-compiler"
  ]
}
