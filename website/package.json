{
  "name": "website",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "otfw dev",
    "audit": "node ../scripts/site-links.mjs",
    "build": "otfw build --ssg && pnpm run audit",
    "preview": "pnpm run build && wrangler dev",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "dependencies": {
    "@opentf/web": "^0.28.0",
    "@opentf/web-docs": "^0.26.0"
  },
  "devDependencies": {
    "@opentf/web-cli": "^1.26.0",
    "wrangler": "^4.122.0"
  }
}
