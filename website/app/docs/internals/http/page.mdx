---
title: "Internals: the HTTP server"
description: What happens to a connection from accept to close — every limit, every default, and what each one costs.
---

# Internals: the HTTP server

This section explains why the HTTP server behaves the way it does and what its
limits cost. Use the focused pages when you need connection lifecycle, isolate
handoff, identity, or comparison details.

## HTTP internals

- [Connections, limits, and protocol](/docs/internals/http/connections) —
  accept, TLS, request bodies, timeouts, and protocol version.
- [Handoff, shutdown, and draining](/docs/internals/http/handoff) — how many
  connections share an isolate and how shutdown completes safely.
- [Identity and observability](/docs/internals/http/identity) — client identity
  and what the server can report.
- [HTTP internals compared](/docs/internals/http/comparison) — trade-offs against
  other runtimes.

The practical API is in the [HTTP server guide](/docs/http); the public contract
is in the [HTTP API reference](/api/http).
