---
title: Debugging
description: A real debugger over the Chrome DevTools Protocol — in the binary that never reaches production.
---

import { Callout } from "@opentf/web-docs";

# Debugging

```sh
esdev --inspect app.ts        # 127.0.0.1:9229
esdev --inspect-brk app.ts    # ...and stop before the first statement
esdev --inspect=9300 app.ts   # a port, an address, or both
```

```text
Debugger listening on ws://127.0.0.1:9229/2c1e5009-be6a-4036-8ad3-185b20bbb1a0
```

Attach with `chrome://inspect`, VS Code, or any CDP client. Sources arrive with
their real `file:` URLs, so a breakpoint set by URL lands in the file you are
looking at; a paused program is genuinely stopped, and locals read through the
debugger.

Use `--inspect-brk` for a program that ends quickly — otherwise it can be over
before a client finishes connecting.

## It is a compile-time opt-in

An inspector port is a **total bypass of the capability model**: attach to one
and you own the isolate, whatever `--deny-all` said. So the code that speaks the
protocol is compiled only into a build that asked for it:

```sh
ES_RUNTIME_INSPECTOR=1 cargo build-dev
```

A build without it accepts `--inspect` and fails with that line, rather than
listening on nothing.

The switch is an environment variable read by a build script, **not a Cargo
feature** — Cargo unifies features across everything built in one invocation, so
a feature declared by `esdev` would be enabled in the `esrun` built beside it. A
feature is declared, and so is on forever; a variable exists only when a human
types it. `esrun`'s own build script refuses to build while it is set, so a
single invocation can never produce both.

## Binding

<Callout type="warn" title="Loopback by default">
  `--inspect=0.0.0.0` is allowed and warned about. A debugger port is a way to
  run code in the process regardless of what it was denied.
</Callout>

`esdev --watch --inspect` reclaims its port across a restart, so you can leave a
debugger attached while you edit.

<Callout type="note" title="Known limitation">
  `console.log` output goes to the terminal, not the debugger's console pane. V8
  emits console events for *its* built-in console, and the runtime's prelude
  replaces that one.
</Callout>
