{
  "name": "shop",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "esdev start",
    "build": "esdev build --minify",
    "start": "esrun --allow-read --allow-write --allow-listen=8080 --allow-net --allow-env=PORT,SHARDS,FAIL_RATE --allow-signals --allow-workers --allow-imports dist/server.js",
    "test": "esdev test"
  },
  "dependencies": {
    "@opentf/micro-ui": "latest"
  }
}
